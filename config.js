// URL base de la API. Déjala vacía ('') si frontend y API están en el mismo dominio (Render).
// Solo necesitas un valor si el frontend está en otro sitio (ej. GitHub Pages).
export const API_URL = '';
